import { ComponentFixture, TestBed } from '@angular/core/testing';

import { UserLoginSummaryComponent } from './user-login-summary.component';

describe('UserLoginSummaryComponent', () => {
  let component: UserLoginSummaryComponent;
  let fixture: ComponentFixture<UserLoginSummaryComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [UserLoginSummaryComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(UserLoginSummaryComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
