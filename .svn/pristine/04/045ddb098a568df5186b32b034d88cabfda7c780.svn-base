import { TestBed } from '@angular/core/testing';

import { LogUtilityService } from './log-utility.service';

describe('LogUtilityService', () => {
  let service: LogUtilityService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(LogUtilityService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
